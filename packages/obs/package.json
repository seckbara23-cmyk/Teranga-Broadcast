{
  "name": "@teranga/obs",
  "version": "0.0.0",
  "private": true,
  "description": "Typed OBS WebSocket v5 client exposing intent-level broadcast control (the ObsClient interface).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(placeholder) lint @teranga/obs\"",
    "test": "echo \"(placeholder) test @teranga/obs\"",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@teranga/core": "workspace:*",
    "@teranga/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "peerDependencies": {
    "obs-websocket-js": "^5.0.6"
  },
  "peerDependenciesMeta": {
    "obs-websocket-js": {
      "optional": true
    }
  }
}
