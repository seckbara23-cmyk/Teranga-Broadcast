/**
 * @teranga/ui — shared UI kit.
 *
 * PLACEHOLDER. Will export shadcn/ui-based components, the Tailwind preset, and
 * the operator-console theme (dark, keyboard-first) used by apps/web — including
 * broadcast-specific pieces (scoreboard editor, replay deck shell, OBS status
 * chips). shadcn components are added via the CLI during Phase 0/1.
 */

export const TERANGA_UI_PLACEHOLDER = true as const;
