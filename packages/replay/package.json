{
  "name": "@teranga/replay",
  "version": "0.0.0",
  "private": true,
  "description": "Replay buffer model and clip/frame math (segment index, in/out marks, frame-accurate cut planning).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(placeholder) lint @teranga/replay\"",
    "test": "echo \"(placeholder) test @teranga/replay\"",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@teranga/core": "workspace:*",
    "@teranga/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
